﻿# NL2SQL 毕设开发进度备忘
> 如果对话丢失，把这个文件发给 Copilot 即可快速恢复上下文。
> 最后更新：2026-03-24
---
## 一、项目基本信息
| 项目 | 内容 |
|---|---|
| 题目 | 基于大模型的自然语言数据库查询系统 |
| 学生 | 张璐加（22330131） |
| 学院 | 人文艺术与数字媒体学院 · 数字媒体技术 |
| 指导教师 | 聂斌玲 |
| 路径 | "算法与精度"引擎专家路径（侧重SQL生成深度、准确率、多数据库适配） |
| 技术栈 | Java 17 + Spring Boot 3.2.5 + LangChain4j 0.35.0 + JSQLParser 4.9 |
| 前端技术栈 | Vue 3.4 + Vite 5 + Axios + highlight.js + ECharts (vue-echarts) |
| 包路径 | com.itheima.n2sql |
| 大模型 | DeepSeek (deepseek-chat, 兼容OpenAI API格式) |
| 数据库 | MySQL / PostgreSQL |
| 构建工具 | Maven (后端) + npm (前端) |

---
## 二、当前系统状态（2026-03-24）

### 🟢 开发已完成，进入论文撰写阶段

### 后端功能清单
| 模块 | 功能 | 文件 |
|------|------|------|
| 数据源管理 | 创建/删除/列表/测试连接/Schema获取 | DataSourceService + DataSourceController |
| Schema提取 | JDBC DatabaseMetaData 扫描表/列/主键/外键/注释 | SchemaExtractService |
| NL2SQL核心 | 大模型调用 + SQL清洗 + 软策略Prompt | Nl2SqlService |
| 查询分类器 | 关键词检测 → 动态追加聚合/多表/嵌套提示(hints) | QueryClassifier |
| 静态校验 | JSQLParser语法验证 + Schema表名列名校验 + 编辑距离建议 | SqlValidateService |
| 动态校验 | SQL真实执行 + 错误捕获 | SqlExecuteService |
| 自纠错循环 | 最多3次重试，完整历史上下文传入大模型 | Nl2SqlService |
| NO_MATCH机制 | 不存在的实体不猜测，返回友好提示 | Nl2SqlService |
| 安全限制 | 只允许SELECT、超时30s、最大1000行 | SqlExecuteService |
| 异常处理 | 全局异常处理器 + 创建数据源连接失败友好提示 | GlobalExceptionHandler + DataSourceService |
| Debug接口 | 绕过大模型直接测试校验/执行/流水线 | DebugController |

### 前端功能清单
| 模块 | 功能 | 文件 |
|------|------|------|
| 数据源管理UI | 侧边栏列表 + 创建弹窗 + 测试连接 + 删除 | Sidebar + DataSourceModal |
| 聊天界面 | Gemini风格，消息气泡，SQL高亮(highlight.js)，欢迎页 | App.vue + ChatMessage |
| 查询结果表格 | 表头/数据行/超过10行折叠/hover高亮 | ChatMessage |
| 图表可视化 | ECharts智能分析 → 柱状图/折线图/饼图自动切换 | ChartDisplay |
| 聊天历史 | 多会话管理，侧边栏历史列表，切换/删除 | App.vue + Sidebar |
| Schema预览 | 右侧抽屉面板，表/列/主键/外键结构化展示 | SchemaDrawer |
| 纠错提示 | 显示自纠错次数 | ChatMessage |

### 测试状态
- test_v2.js：精确验证测试 27/27 通过（结果正确性 + 多种查询场景）
- test_v3.js：校验机制专项测试 28/28 通过（静态校验 + 动态执行 + 模拟纠错流水线）

---
## 三、系统架构总览

### 完整数据流
```
用户输入: "查询销售额最高的前5个产品" + 数据源ID
    ↓
1. SchemaExtractService → 自动扫描数据库结构(含字段类型+注释) → DDL文本
    ↓
2. QueryClassifier.detectHints() → 检测关键词 → 返回补充指令列表(hints)
   (软策略：不做互斥分类，多种提示可同时叠加)
    ↓
3. PromptTemplateService.buildSoftPrompt()
   → base_nl2sql.txt 基础模板 + {{hints}} + {{schema}} + {{question}}
    ↓
4. ChatLanguageModel → 发给DeepSeek大模型 → 返回SQL文本
    ↓
5. SqlCleanUtil → 去掉markdown格式、多余文字 → 干净的SQL
    ↓
6. NO_MATCH 检测 → 如果大模型返回"NO_MATCH: 原因" → 直接返回友好提示
    ↓（不是NO_MATCH，继续）
7. SqlValidateService → JSQLParser语法检查 + Schema校验(Levenshtein纠错建议)
    ↓ 失败          ↓ 通过
    ↓           8. SqlExecuteService → 数据库真实执行
    ↓               ↓ 失败          ↓ 成功 → 返回结果 + 图表可视化
    ↓               ↓
    ↓←←←←←←←←←←←←←↓
   记录到 correctionHistory（完整历史）
   → buildCorrectionPromptWithHistory()
   → 把所有之前失败的SQL和错误信息传给大模型
   → 大模型重新生成 → 回到步骤6（最多重试3次）
```

### 项目分包结构
```
com.itheima.n2sql/
├── N2sqlApplication.java              ← 启动入口
├── config/
│   ├── DataSourceManager.java         ← 动态连接池管理(HikariCP)
│   ├── LlmProperties.java            ← 从yml读取大模型配置
│   └── LlmConfig.java                ← 创建大模型客户端Bean
├── exception/
│   ├── BizException.java             ← 自定义业务异常
│   └── GlobalExceptionHandler.java   ← 全局异常捕获
├── model/
│   ├── enums/DbType.java            ← 数据库类型枚举(MYSQL/POSTGRESQL)
│   ├── entity/DataSourceInfo.java    ← 数据源实体
│   └── dto/                          ← 数据传输对象
│       ├── ApiResult.java            ← 统一响应格式
│       ├── DataSourceCreateRequest/TestResponse.java
│       ├── ColumnInfo/TableSchema/DatabaseSchema.java
│       ├── Nl2SqlRequest/Response.java
│       ├── QueryResult.java
│       └── CorrectionRecord.java
├── service/
│   ├── DataSourceService.java        ← 数据源增删查+测试连接
│   ├── SchemaExtractService.java     ← JDBC自动扫描数据库结构
│   ├── SqlValidateService.java       ← JSQLParser语法验证+Schema校验+Levenshtein纠错
│   ├── SqlExecuteService.java        ← 安全执行SQL
│   ├── QueryClassifier.java          ← 查询意图检测器(软策略)
│   ├── Nl2SqlService.java            ← 核心服务(软策略+完整历史纠错+NO_MATCH)
│   └── prompt/
│       └── PromptTemplateService.java ← 提示词模板管理
├── util/
│   └── SqlCleanUtil.java             ← SQL清洗工具
└── controller/
    ├── DataSourceController.java     ← 数据源管理接口(6个API)
    ├── Nl2SqlController.java         ← NL2SQL核心接口(1个API)
    └── DebugController.java          ← 校验调试接口(4个API)

resources/
├── application.yml                    ← 主配置
├── application-dev.yml                ← 开发环境配置
├── init_test_db.sql                   ← 电商测试数据库(6张表)
├── init_test_db_education.sql         ← 教育测试数据库
├── init_test_db_hospital.sql          ← 医院测试数据库
└── prompts/
    ├── base_nl2sql.txt               ← 基础NL2SQL提示词模板(含{{hints}}+NO_MATCH约束)
    └── correction_nl2sql.txt         ← 纠错提示词模板(含{{error_history}}完整历史)

frontend/src/
├── main.js                            ← Vue应用入口
├── App.vue                            ← 主页面(聊天历史管理+Schema按钮)
├── api/index.js                       ← Axios封装(统一响应处理)
└── components/
    ├── Sidebar.vue                    ← 侧边栏(数据源+聊天历史)
    ├── ChatMessage.vue                ← 消息组件(SQL高亮+表格+图表)
    ├── ChartDisplay.vue               ← ECharts图表可视化
    ├── DataSourceModal.vue            ← 数据源创建弹窗
    └── SchemaDrawer.vue               ← Schema预览抽屉面板
```

---
## 四、API 接口清单

### 数据源管理 `/api/datasource`
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/datasource | 创建数据源 |
| GET | /api/datasource | 获取所有数据源列表 |
| GET | /api/datasource/{id} | 获取单个数据源 |
| DELETE | /api/datasource/{id} | 删除数据源 |
| GET | /api/datasource/{id}/schema | 获取数据库Schema结构 |
| POST | /api/datasource/test | 测试数据源连接 |

### NL2SQL核心 `/api/nl2sql`
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/nl2sql | 自然语言转SQL并执行 |

### 调试接口 `/api/debug`
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/debug/validate | 直接测试静态校验 |
| POST | /api/debug/execute | 直接测试SQL执行 |
| POST | /api/debug/full-pipeline | 测试完整校验→执行流水线 |
| GET | /api/debug/schema/{dsId} | 查看Schema DDL |

---
## 五、核心技术要点

### 5.1 软策略提示词工程
- 不做互斥分类，用一个基础模板兜底
- QueryClassifier.detectHints() 检测关键词，返回补充指令列表
- 三组hints可同时叠加：聚合提示 + 多表联查提示 + 嵌套/高级查询提示
- 基础模板 base_nl2sql.txt 中 {{hints}} 占位符动态替换

### 5.2 自验证机制（两级校验 + 自纠错循环）
- **第一级**：JSQLParser 语法验证 + Schema 校验（表名/列名比对 + Levenshtein编辑距离建议）
- **第二级**：数据库真实执行 + 错误捕获
- **自纠错**：失败时记录到 correctionHistory，用完整历史构建纠错prompt重新生成（最多3次）
- **NO_MATCH**：大模型检测到不存在的实体时返回 NO_MATCH，系统不猜测

### 5.3 复杂SQL处理策略
- 简单PlainSelect：正常做表名+列名静态校验
- 子查询/UNION/窗口函数等复杂SQL：跳过列名静态校验，交给数据库执行阶段 + 纠错闭环

---
## 六、关键配置速查

### application-dev.yml
```yaml
n2sql:
  llm:
    api-key: sk-fc58354d112e4b79aaa689bb3e56cdee
    base-url: https://api.deepseek.com/v1
    model-name: deepseek-chat
    temperature: 0.0
    max-tokens: 2048
  correction:
    max-retries: 3
  sql:
    max-rows: 1000
    timeout-seconds: 30
```

### 测试数据库
- MySQL 测试库：n2sql_test（电商场景，6张表）
- MySQL 密码：Zlj2004826
- 数据源为内存存储，重启后需重新创建

### pom.xml 关键依赖
- spring-boot-starter-web/validation/jdbc (3.2.5)
- langchain4j + langchain4j-open-ai + langchain4j-spring-boot-starter (0.35.0)
- jsqlparser (4.9)
- mysql-connector-j + postgresql
- lombok

---
## 七、后续计划
| 任务 | 优先级 | 状态 |
|------|--------|------|
| 撰写毕业论文初稿 | 🔴 最高 | 进行中 |
| CSV导出功能（可选） | 🟢 低 | 待定 |
| 纠错历史详情展示（可选） | 🟢 低 | 待定 |
| Spider/BIRD 评测集测试（可选） | 🟡 中 | 待定 |
