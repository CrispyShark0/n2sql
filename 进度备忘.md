# NL2SQL 毕设开发进度备忘
> 如果对话丢失，把这个文件发给 Copilot 即可快速恢复上下文。
---
## 一、项目基本信息
| 项目 | 内容 |
|---|---|
| 方向 | 基于大模型的自然语言数据库查询系统 |
| 路径 | "算法与精度"引擎专家路径 |
| 技术栈 | Java 17 + Spring Boot 3.2.5 + LangChain4j 0.35.0 + JSQLParser 4.9 |
| 包路径 | com.itheima.n2sql |
| 大模型 | DeepSeek (兼容OpenAI API格式) |
| 数据库 | MySQL / PostgreSQL |
| 构建工具 | Maven |
| 开发约定 | Java基础一般，每步先讲解知识再动手，一步步确认推进 |
---
## 二、导师要求（开题报告细分）
我的方向是"算法与精度"引擎专家路径，和同学徐枫哲做区分：
- 我(张璐加)：侧重 SQL生成深度、准确率、多数据库适配，打造"数据提取工具"
- 徐枫哲：侧重 智能分析师、RAG增强、归因分析、可视化报告
导师对我的具体要求：
1. 强化复杂SQL处理（多表联查、嵌套查询、开窗函数）通过提示词工程
2. 自纠错与自反馈机制（SQL执行报错自动反馈大模型迭代修正）
3. 多数据库方言支持（MySQL、PostgreSQL、ClickHouse甚至NoSQL）
4. 使用 Spider/BIRD 评测集量化执行准确率
---
## 三、系统架构总览
### 完整数据流（2026-03-18 最新版 — 软策略 + 完整历史纠错 + NO_MATCH）
`
用户输入: "查询销售额最高的前5个产品" + 数据源ID
    |
    v
1. SchemaExtractService -> 自动扫描数据库结构(含字段类型+注释) -> DDL文本
    |
    v
2. QueryClassifier.detectHints() -> 检测关键词 -> 返回补充指令列表(hints)
   (软策略：不做互斥分类，多种提示可同时叠加)
    |
    v
3. PromptTemplateService.buildSoftPrompt() 
   -> base_nl2sql.txt 基础模板 + {{hints}} 动态替换 + {{schema}} + {{question}}
   (始终用一个基础模板兜底，hints为空时等于简单查询)
    |
    v
4. ChatLanguageModel -> 发给DeepSeek大模型 -> 返回SQL文本
    |
    v
5. SqlCleanUtil -> 去掉markdown格式、多余文字 -> 干净的SQL
    |
    v
6. NO_MATCH 检测 -> 如果大模型返回"NO_MATCH: 原因" -> 直接返回友好提示，不执行
    |（不是NO_MATCH，继续）
    v
7. SqlValidateService -> JSQLParser本地语法检查 + Schema校验(Levenshtein纠错)
    |         |
    | 失败    | 通过
    |         v
    |    8. SqlExecuteService -> 数据库真实执行
    |         |         |
    |    失败  |         | 成功
    |         |         v
    v         v    返回结果
  记录到 correctionHistory（完整历史）
  -> PromptTemplateService.buildCorrectionPromptWithHistory()
  -> 把【所有】之前失败的SQL和错误信息传给大模型（避免来回振荡）
  -> 大模型重新生成 -> 回到6（NO_MATCH检测）
                    (最多重试3次)
`
### 项目分包结构
`
com.itheima.n2sql/
|-- N2sqlApplication.java              <- 启动入口
|
|-- config/                            <- 配置层
|   |-- DataSourceManager.java         <- 动态连接池管理(HikariCP)
|   |-- LlmProperties.java            <- 从yml读取大模型配置
|   |-- LlmConfig.java                <- 创建大模型客户端Bean
|
|-- exception/                         <- 异常处理层
|   |-- BizException.java             <- 自定义业务异常
|   |-- GlobalExceptionHandler.java   <- 全局异常捕获
|
|-- model/                             <- 数据模型层
|   |-- enums/DbType.java            <- 数据库类型枚举
|   |-- entity/DataSourceInfo.java    <- 数据源实体
|   |-- dto/                          <- 数据传输对象(共10个)
|       |-- ApiResult.java            <- 统一响应格式
|       |-- DataSourceCreateRequest/TestResponse.java
|       |-- ColumnInfo/TableSchema/DatabaseSchema.java
|       |-- Nl2SqlRequest/Response.java
|       |-- QueryResult.java
|       |-- CorrectionRecord.java
|
|-- service/                           <- 业务服务层
|   |-- DataSourceService.java        <- 数据源增删查+测试连接
|   |-- SchemaExtractService.java     <- JDBC自动扫描数据库结构(含字段类型+注释)
|   |-- SqlValidateService.java       <- JSQLParser语法验证+Schema校验+Levenshtein纠错
|   |-- SqlExecuteService.java        <- 安全执行SQL
|   |-- QueryClassifier.java          <- 查询意图检测器(软策略：detectHints()返回补充指令)
|   |-- Nl2SqlService.java            <- 核心服务(软策略+完整历史纠错+NO_MATCH检测)
|   |-- prompt/
|       |-- PromptTemplateService.java <- 提示词模板管理(buildSoftPrompt+buildCorrectionPromptWithHistory)
|
|-- util/
|   |-- SqlCleanUtil.java             <- SQL清洗工具
|
|-- controller/                        <- 接口层
|   |-- DataSourceController.java     <- 数据源管理接口(5个API)
|   |-- Nl2SqlController.java         <- NL2SQL核心接口(1个API)
|
resources/
|-- application.yml                    <- 主配置
|-- application-dev.yml                <- 开发环境配置
|-- prompts/
    |-- base_nl2sql.txt               <- 基础NL2SQL提示词模板(含{{hints}}占位符+NO_MATCH约束)
    |-- correction_nl2sql.txt         <- 纠错提示词模板(含{{error_history}}占位符，支持完整历史)
`
---
## 四、已完成阶段详细记录
### 第一阶段：基础架构（已完成）
> 解决的问题：系统怎么连数据库、怎么知道数据库长什么样
> 一句话：搭地基
**Step 1.1 分包 + yml 配置**
- application.yml：主配置（端口8080、日志级别）
- application-dev.yml：开发环境配置（API Key、模型地址、纠错重试次数、SQL安全限制）
- N2sqlApplication.java：排除了 DataSourceAutoConfiguration（因为用动态数据源）
- 知识点：Spring Profile 多环境配置、@SpringBootApplication 的 exclude 机制
**Step 1.2 全局异常 + 统一响应**
- ApiResult<T>：统一响应格式 {code, message, data}，泛型支持任意data类型
- BizException：自定义业务异常，支持 code+message、message+cause 两种构造
- GlobalExceptionHandler：@RestControllerAdvice 全局捕获，处理3类异常(业务/校验/未知)
- 知识点：@RestControllerAdvice、@ExceptionHandler、异常链(cause)
**Step 1.3 数据源模型层**
- DbType 枚举：MYSQL/POSTGRESQL，自带默认端口、URL模板、驱动名、buildJdbcUrl()
- DataSourceInfo 实体：数据源的"名片"，含 id/name/dbType/host/port/dbName/username/password
- DataSourceCreateRequest DTO：前端提交的创建表单，带 @NotBlank/@NotNull 校验
- DataSourceTestResponse DTO：测试连接结果，静态工厂方法 ok()/fail()
- 知识点：枚举带参数、Entity vs DTO 的区别、@Builder 链式构建、参数校验注解
**Step 1.4 数据源服务层**
- DataSourceManager：ConcurrentHashMap 管理多个 HikariDataSource 连接池
  - register() 创建连接池、getConnection() 获取连接、remove() 关闭连接池
- DataSourceService：业务层封装，数据暂存内存(ConcurrentHashMap)
  - create() 生成UUID+注册连接池、listAll()、getById()、delete()、testConnection()
- 知识点：HikariCP 连接池、ConcurrentHashMap 线程安全、@RequiredArgsConstructor 依赖注入
**Step 1.5 Schema 提取**
- ColumnInfo DTO：列名、数据类型(如VARCHAR(50))、是否可空、注释
- TableSchema DTO：表名、表注释、列列表、主键列表、外键列表(格式 "user_id -> users.id")
- DatabaseSchema DTO：数据库名 + 所有表。核心方法 formatAsDDL() 转成 CREATE TABLE 文本
- SchemaExtractService：通过 JDBC DatabaseMetaData API 自动扫描
  - getTables() 获取表名、getColumns() 获取列、getPrimaryKeys()、getImportedKeys() 获取外键
- 知识点：JDBC DatabaseMetaData、为什么转DDL格式(大模型最擅长理解)、try-with-resources
---
### 第二阶段：LLM对接与基础NL2SQL（已完成）
> 解决的问题：怎么把中文翻译成SQL并执行
> 一句话：装大脑
**Step 2.1 LLM 对接**
- LlmProperties：@ConfigurationProperties(prefix="n2sql.llm") 从yml读取配置
  - apiKey、baseUrl、modelName、temperature(0.0=精确)、maxTokens(2048)
- LlmConfig：@Configuration + @Bean 创建 OpenAiChatModel（兼容DeepSeek API）
  - 超时60秒、开发阶段开启请求/响应日志
- 知识点：@ConfigurationProperties 自动绑定、@Bean 注册Spring管理对象、yml横杠转驼峰
**Step 2.2 提示词模板**
- base_nl2sql.txt：基础提示词模板文件，2个变量占位符
  - {{schema}} <- SchemaExtractService 自动扫描的DDL文本(不是从用户问题提取)
  - {{question}} <- 用户输入的原始自然语言问题
  - 规则：只生成SELECT、只用schema中存在的表和列、用JOIN、用聚合函数等
- PromptTemplateService：@PostConstruct 启动时加载模板到HashMap缓存
  - buildNl2SqlPrompt(schemaDDL, question) 替换占位符生成完整提示词
- 知识点：提示词工程(不是RAG)、@PostConstruct 初始化、ClassPathResource 读取资源文件
**Step 2.3 核心NL2SQL服务**
- SqlCleanUtil：静态工具类，从大模型返回文本提取干净SQL
  - cleanSql() 处理 `sql...` 包裹、多余空白、末尾分号
  - isSelectStatement() 检查是否以SELECT开头
- Nl2SqlRequest DTO：dataSourceId + question（都带 @NotBlank）
- Nl2SqlResponse DTO：question + generatedSql + queryResult + success + errorMessage
  - 预留字段：retryCount、correctionHistory（第三阶段启用）
- QueryResult DTO：columns(列名列表) + rows(List<Map>) + rowCount + executeTimeMs
- CorrectionRecord DTO：sql + errorType + errorMessage + timestamp（第三阶段启用）
- Nl2SqlService：核心流程 Schema提取 -> 提示词构建 -> 大模型调用 -> SQL清洗 -> 执行
**Step 2.4 SQL 执行引擎**
- SqlExecuteService：安全执行SQL，三重防护
  - 只允许SELECT（isSelectStatement检查）
  - 超时限制（@Value 从yml读取，默认30秒）
  - 行数限制（默认1000行）
- convertResultSet()：JDBC ResultSet -> QueryResult（LinkedHashMap保持列顺序）
- 知识点：@Value 读取单个配置、Statement 的 setQueryTimeout/setMaxRows、ResultSetMetaData
---
### 第三阶段：自纠错机制（已完成）
> 解决的问题：大模型生成的SQL有错怎么办
> 一句话：加容错
**Step 3.1 SQL语法静态验证（JSQLParser）**
- SqlValidateService：用 JSQLParser 在本地做语法预检（不需要连数据库）
  - validate(sql) 方法返回 ValidationResult（内部类）
  - 验证逻辑：空值检查 -> CCJSqlParserUtil.parse(sql) 解析语法 -> 确认是SELECT
  - extractErrorMessage() 截断过长错误信息（最多200字符）
- ValidationResult 内部类：valid(boolean) + errorMessage(String)
  - 静态工厂方法 ok() / fail(msg)、getter: isValid() / getErrorMessage()
- 能发现：括号不匹配、关键字拼错、语法结构错误
- 发现不了：表名/列名不存在（要靠数据库执行才知道）
- 知识点：JSQLParser 的 CCJSqlParserUtil.parse()、instanceof Select、内部类
**Step 3.2 自纠错提示词模板**
- correction_nl2sql.txt：纠错专用提示词模板，4个变量占位符
  - {{schema}} <- 数据库结构DDL（和首次生成一样）
  - {{question}} <- 用户原始问题
  - {{previous_sql}} <- 上一轮生成的错误SQL
  - {{error_message}} <- JSQLParser或数据库返回的错误信息
  - 比 base_nl2sql.txt 多了"上一轮错误SQL"和"错误信息"
- PromptTemplateService 修改：
  - init() 新增加载 correction_nl2sql 模板
  - 新增 buildCorrectionPrompt(schemaDDL, question, previousSql, errorMessage) 方法
- 知识点：大模型没有记忆，必须每次把完整上下文都传过去
**Step 3.3 执行反馈闭环服务**
- Nl2SqlService.java 全面改造（从"一次性生成"变成"循环纠错"）
- 新增依赖注入：SqlValidateService
- 新增配置读取：@Value("n2sql.correction.max-retries") 最大重试次数（默认3）
- 核心循环：for(attempt=0; attempt<=maxRetries; attempt++)
  - 第一关：SqlValidateService 语法验证
  - 第二关：SqlExecuteService 数据库执行
  - 任何一关失败 -> 记录CorrectionRecord -> requestCorrection() 纠错
- 新增私有方法：
  - requestCorrection(schemaDDL, question, previousSql, errorMessage, retryNum)
    构建纠错提示词 -> 调用大模型 -> 清洗SQL -> 返回修正后的SQL
  - buildFailResponse(question, lastSql, errorMessage, history)
    构建统一的失败响应（避免重复代码）
- CorrectionRecord 和 Nl2SqlResponse.correctionHistory 正式启用
  - errorType: "SYNTAX_ERROR" 或 "EXECUTION_ERROR"
- 知识点：for循环+continue实现重试、提取私有方法减少重复
---
## 五、当前系统状态
### 编译状态：全部25个Java文件零编译错误，前端编译零错误
### 代码 Review 与修复记录（2026-02-28）
- 逐文件讲解+Review，详见 代码讲解与Review.md（30个文件/模块，829行文档）
- 发现并修复 SqlValidateService.java 的 JSQLParser 4.9 API 兼容问题：
  - SelectBody 类在 4.9 中已移除 -> 改为直接 instanceof PlainSelect
  - SelectExpressionItem 在 4.9 中已合并到 SelectItem -> 改用 item.getExpression()
  - 修复后 mvnw compile BUILD SUCCESS，零错误
- 剩余 WARNING（不影响功能）：
  - TablesNamesFinder.getTableList() 被标记为 deprecated（仍可用）
  - Javadoc 空行警告
  - 部分方法暂未被调用（等 Controller 层写好就会用到）

### 复杂嵌套查询 Bug 修复（2026-03-16）
- 问题现象：查询"销售额最高的产品类别，以及该类别下卖得最好的产品"等复杂问题会报错
- 根本原因：SqlValidateService 的 validateSchema() 方法中：
  - `buildAliasMap()` 和 `extractColumns()` 只处理 `PlainSelect`（简单 SELECT）
  - 当大模型生成含子查询（如 `SELECT * FROM (SELECT ... ROW_NUMBER() ...) sub`）、
    UNION、窗口函数等复杂 SQL 时，静态校验会误判或抛异常
- 修复方案：
  1. 表名校验包裹 try-catch，TablesNamesFinder 对复杂 SQL 异常时跳过表名校验
  2. 列名校验增加智能判断：
     - 如果 SELECT 语句不是 PlainSelect（如 UNION）→ 跳过列名校验
     - 如果 FROM 子句是子查询（如 `SELECT * FROM (SELECT ...) sub`）→ 跳过列名校验
  3. 新增 `getOutermostPlainSelect()` 辅助方法
  4. 复杂查询的验证交给数据库执行阶段 + 自纠错反馈闭环处理

### 前端界面改版v1（2026-03-16）→ 已被 Gemini 风格 v2 替代
- 从深色主题全面改为蓝白科技风（Blue-White Tech Theme）
- 2026-03-18 再次改版为 Gemini 风格，详见"前端界面改版v2"章节
- 改动文件：App.vue、Sidebar.vue、ChatMessage.vue、DataSourceModal.vue、index.html
- 主要变化（已废弃，仅供历史参考）：
  - 主背景：`#212121`(黑) → `#f0f4f8`(淡灰蓝渐变)
  - 侧边栏：`#171717`(纯黑) → `#1e3a5f→#152d4a`(深蓝渐变)，保持深浅对比
  - 主色调：`#10a37f`(绿) → `#3b82f6`(蓝)，所有按钮、发送键、输入框用蓝色渐变
  - 标题文字：使用蓝色渐变彩色字效果
  - SQL代码块头部：深蓝渐变 `#1e3a5f→#1e40af`，代码背景保持 `#0f172a`（深色对比）
  - 结果表格：白色背景 + 蓝色边框 + 悬浮蓝色高亮
  - 弹窗：白色背景 + 蓝色光晕聚焦效果
  - 添加 Google Fonts Inter 字体
  - 整体风格：毛玻璃(backdrop-filter)、蓝色投影(box-shadow)、渐变按钮

### 功能完整性
六个阶段完成后，又经过两轮重要改进（按导师反馈 + 前端Gemini风格改版），
系统已具备从"用户说中文"到"返回数据库查询结果"的完整链路，
并且带有：软策略提示词工程、完整历史自纠错、NO_MATCH实体检测、Gemini风格前端交互。

### 当前缺少什么
- 缺少量化评测数据 -> 需要第七阶段 Spider/BIRD 评测
---
## 六、后续开发计划
| 阶段 | 目的 | 优先级 | 状态 |
|---|---|---|---|
| 第四阶段 | 复杂SQL支持（查询分类+4套模板） | 中 | ✅ 已完成 → 已被软策略替代 |
| 第五阶段 | 多数据库方言适配 | 中 | ✅ 已完成 |
| 第六阶段 | REST API接口 + 前端界面 | 高 | ✅ 已完成 |
| Bug修复 | 复杂嵌套查询静态校验误判 | 高 | ✅ 已修复(2026-03-16) |
| 前端改版v1 | 蓝白科技风UI | 中 | ✅ 已完成(2026-03-16) → 已被Gemini风格替代 |
| 导师反馈改进 | 软策略+完整历史纠错+NO_MATCH | 高 | ✅ 已完成(2026-03-18) |
| 前端改版v2 | Gemini风格UI | 中 | ✅ 已完成(2026-03-18) |
| 第七阶段 | 评测与优化(Spider/BIRD) | 高 | 待开始 |
### 实测状态（2026-03-04）
- DeepSeek API Key 已配置：sk-fc58354d112e4b79aaa689bb3e56cdee
- MySQL 测试数据库已创建：n2sql_test（6张表：departments/employees/customers/products/orders/order_items）
- MySQL 密码：Zlj2004826
- 端到端测试 4/4 全部通过（简单查询、聚合+JOIN、多表TOPN、嵌套子查询）
- 数据源ID示例：ds-0c9d2c5f（内存存储，重启后需重新注册）

### 下一步：第七阶段 Spider 评测集量化测试（最高优先级）
- 详见：项目进度总结与含金量分析.md

### 前端技术栈
- Vue 3.4 + Vite 5 + Axios + highlight.js + ECharts
- 组件：App.vue（主页面）、Sidebar.vue（侧边栏）、ChatMessage.vue（消息组件）、DataSourceModal.vue（数据源弹窗）
- 配色方案：Gemini 风格（纯白 + Google蓝）
  - 主色：#1a73e8（Google蓝）
  - 侧边栏：#f8f9fa（浅灰白）
  - 主区域：#ffffff（纯白）
  - 字体：Google Sans / Inter + 系统字体栈
  - AI头像：✦ 星形符号 + 蓝色渐变圆形
  - 输入框：胶囊圆角(28px) + 圆形发送按钮
  - 示例卡片：胶囊chip横排（非卡片网格）
  - 按钮风格：pill形(border-radius:100px)
---
### 第四阶段：复杂SQL支持（已完成 → 已被软策略替代）
> 解决的问题：同一个提示词处理所有问题，复杂查询准确率低
> 一句话：分诊治疗
> ⚠️ 2026-03-18 按导师反馈改为"软策略"：不再硬分类选模板，而是用基础模板兜底+动态追加补充指令

**原方案（已废弃）：4套专用模板 + 硬分类**
- QueryType 枚举分4种、QueryClassifier 互斥分类、4个专用prompt模板
- 问题：误分类导致选错模板，复杂问题可能同时涉及聚合+多表+嵌套

**新方案（当前生效）：软策略 — 详见"导师反馈改进"章节**
- QueryClassifier.detectHints() 返回补充指令列表，可同时叠加
- 始终用 base_nl2sql.txt 基础模板，通过 {{hints}} 占位符动态注入

**Step 4.1 查询意图枚举 QueryType**
- model/enums/QueryType.java
- 四种类型：SIMPLE（简单）、AGGREGATE（聚合统计）、MULTI_JOIN（多表联查）、NESTED（嵌套/高级）
- 每种类型关联一个专用提示词模板名称

**Step 4.2 查询意图分类器 QueryClassifier**
- service/QueryClassifier.java
- 基于关键词匹配+正则表达式，不调用大模型（快速、免费、可控）
- 优先级：NESTED > AGGREGATE > MULTI_JOIN > SIMPLE
- 支持 Schema 辅助判断（如果问题提到了2张以上表名/表注释 → 多表联查）
- 聚合+多表组合 → 自动升级为 NESTED

**Step 4.3 四套专用提示词模板**
- resources/prompts/simple_nl2sql.txt — 强调单表、简单WHERE
- resources/prompts/aggregate_nl2sql.txt — 强调聚合函数、GROUP BY、HAVING、别名
- resources/prompts/multijoin_nl2sql.txt — 强调JOIN、外键关系、表别名、列限定
- resources/prompts/nested_nl2sql.txt — 强调窗口函数、子查询、ROW_NUMBER、NOT EXISTS

**Step 4.4 PromptTemplateService 改造**
- init() 新增加载4个专用模板（总共6个模板）
- 新增 buildPromptByTemplateName(name, schemaDDL, question) 方法
- 找不到指定模板时自动回退到 base_nl2sql（容错）

**Step 4.5 Nl2SqlService 集成**
- 注入 QueryClassifier
- 生成SQL前先分类，根据分类结果选择对应提示词模板
---
### 第五阶段：多数据库方言适配（已完成）
> 解决的问题：MySQL 和 PostgreSQL 语法差异导致生成错误的 SQL
> 一句话：因地制宜

**Step 5.1 DatabaseSchema 增加 dbType 字段**
- 新增 dbType 字段（如 "MySQL"、"PostgreSQL"）
- formatAsDDL() 输出头部增加数据库类型标识

**Step 5.2 SchemaExtractService 自动检测数据库类型**
- 使用 metaData.getDatabaseProductName() 自动获取数据库产品名称
- 构建 DatabaseSchema 时设置 dbType

**Step 5.3 PromptTemplateService 方言注入**
- buildPromptByTemplateName 新增 dbType 参数重载
- 如果 dbType 不为空，在提示词末尾追加方言提示
- "IMPORTANT: Generate SQL compatible with {dbType} syntax"

**Step 5.4 Nl2SqlService 传递方言信息**
- 调用 buildPromptByTemplateName 时传入 schema.getDbType()
---
### 第六阶段：REST API 接口（已完成）
> 解决的问题：系统有了核心功能但外部无法调用
> 一句话：开门营业

**Step 6.1 DataSourceController**
- 路径前缀：/api/datasource
- POST /api/datasource — 创建数据源（@Valid + @RequestBody）
- GET /api/datasource — 获取所有数据源列表
- GET /api/datasource/{id} — 根据ID获取单个数据源（@PathVariable）
- DELETE /api/datasource/{id} — 删除数据源
- POST /api/datasource/test — 测试数据源连接
- 知识点：@RestController、@RequestMapping、@PostMapping/@GetMapping/@DeleteMapping

**Step 6.2 Nl2SqlController**
- 路径前缀：/api/nl2sql
- POST /api/nl2sql — 自然语言转SQL并执行（核心接口）
  - 请求体：{"dataSourceId":"xxx", "question":"查询..."}
  - 返回：生成的SQL + 执行结果 + 纠错历史
- 即使 NL2SQL 失败也返回 HTTP 200，前端根据 response.success 判断
---
### Bug 修复：复杂嵌套查询静态校验误判（2026-03-16）
> 解决的问题：复杂嵌套 SQL（子查询、UNION、窗口函数）被静态校验误杀
> 一句话：让验证器更聪明

**问题描述**
- 用户提问"查询销售额最高的产品类别，以及该类别下卖得最好的产品"会报错
- 大模型生成的嵌套 SQL（如 `SELECT * FROM (SELECT ... ROW_NUMBER() ...) sub`）
  被 SqlValidateService 的列名校验误判为"列不存在"

**根因分析**
- `buildAliasMap()` 只处理 `PlainSelect`（简单 SELECT），子查询包裹的外层 SELECT 无法提取别名
- `extractColumns()` 同样只处理 `PlainSelect`，无法正确解析子查询中的列引用
- `TablesNamesFinder` 对某些复杂 SQL 可能抛异常

**修复内容（SqlValidateService.java）**
1. 表名校验：`TablesNamesFinder.getTableList()` 包裹 try-catch，异常时跳过校验
2. 列名校验：增加三层智能判断
   - 不是 `PlainSelect`（如 UNION）→ 跳过列名校验
   - FROM 子句是子查询（非 Table）→ 跳过列名校验
   - 只有简单的 `PlainSelect` 且 FROM 是真实表 → 正常校验
3. 新增 `getOutermostPlainSelect(Select)` 辅助方法
4. 复杂查询的验证完全交给数据库执行阶段 + 自纠错反馈闭环

---
### 前端界面改版：蓝白科技风（2026-03-16）
> 解决的问题：深色界面不够高级，配色不够科技感
> 一句话：换新装

**改动文件清单**
- `frontend/index.html` — 新增 Google Fonts Inter 字体引入
- `frontend/src/App.vue` — 全局样式 + 主区域样式全面重写
- `frontend/src/components/Sidebar.vue` — 侧边栏样式重写
- `frontend/src/components/ChatMessage.vue` — 消息卡片/表格样式重写
- `frontend/src/components/DataSourceModal.vue` — 弹窗样式重写

**配色方案**
| 元素 | 旧色值 | 新色值 |
|------|--------|--------|
| 主背景 | #212121 (黑) | #f0f4f8 (淡灰蓝渐变) |
| 侧边栏 | #171717 (纯黑) | #1e3a5f→#152d4a (深蓝渐变) |
| 主色调 | #10a37f (绿) | #3b82f6 (蓝) |
| 文字色 | #ececf1 (白) | #1e293b (深灰) |
| SQL块头部 | #1e1e2e (暗紫) | #1e3a5f→#1e40af (深蓝渐变) |
| SQL代码背景 | #0d1117 (黑) | #0f172a (深海蓝) |
| 表格背景 | #161b22 (暗) | #ffffff (白) |
| 弹窗背景 | #1e1e1e (暗) | #ffffff (白) |
| 发送按钮 | #10a37f (绿) | #3b82f6→#2563eb (蓝渐变) |

**视觉特效**
- 毛玻璃效果：`backdrop-filter: blur()` 用于状态栏、消息区、输入框
- 蓝色投影：所有按钮和头像使用 `box-shadow` 蓝色光晕
- 渐变标题：`background: linear-gradient(...)` + `-webkit-background-clip: text`
- 悬浮动效：卡片悬浮时 `translateY(-2px)` + 阴影增强
- 聚焦光环：输入框聚焦时蓝色边框 + 发光阴影

---
### 导师反馈改进（2026-03-18，3项重大改进）
> 解决的问题：导师对系统提出4点改进意见，逐一落实
> 一句话：精雕细琢

**导师原话总结：**
1. Schema 多提取字段类型和注释 → 原本就已完成（ColumnInfo 含 dataType + comment）
2. 意图分类改为软策略，避免误分类 → 新实现
3. 重试时传入完整历史错误上下文 → 新实现
4. 不存在的实体返回 NO_MATCH → 新实现

**改进1：软策略（替代旧的硬分类）**
- 文件：`QueryClassifier.java`
- 旧方案：`classify(question, schema)` → 返回 QueryType 枚举（互斥四选一）
- 新方案：`detectHints(question, schema)` → 返回 `List<String>` 补充指令列表
- 三组关键词检测可同时命中：聚合关键词 + 多表关键词 + 嵌套关键词
- 新增 `mentionsMultipleTables()` 辅助方法（从 Schema 表名/注释判断是否多表）
- 新增 `matchesAny()` 辅助方法（正则+包含双模式匹配）
- 文件：`base_nl2sql.txt` 新增 `{{hints}}` 占位符
- 文件：`PromptTemplateService.java` 新增 `buildSoftPrompt()` 方法
- 文件：`Nl2SqlService.java` 改用 `detectHints()` + `buildSoftPrompt()` 流程
- 旧的4套专用模板(simple/aggregate/multijoin/nested)不再使用

**改进2：完整历史纠错（替代旧的单次纠错）**
- 文件：`correction_nl2sql.txt` 改用 `{{error_history}}` 占位符（替代旧的 `{{previous_sql}}` + `{{error_message}}`）
- 文件：`PromptTemplateService.java` 新增 `buildCorrectionPromptWithHistory()` 方法
  - 遍历 `List<CorrectionRecord>`，格式化为 "### Attempt 1\nSQL:...\nError:..." 形式
  - 大模型能看到所有之前失败的尝试，避免来回振荡
- 文件：`Nl2SqlService.java` 中 `requestCorrectionWithHistory()` 传入完整 correctionHistory
- 旧的 `buildCorrectionPrompt(schemaDDL, question, previousSql, errorMessage)` 方法已删除

**改进3：NO_MATCH 实体不存在检测**
- 文件：`base_nl2sql.txt` 规则11 — 明确约束大模型不猜测不存在的实体
- 文件：`correction_nl2sql.txt` 规则9 — 纠错时也有同样约束
- 文件：`Nl2SqlService.java` 新增：
  - `isNoMatch(text)` — 检测响应是否以 "NO_MATCH" 开头
  - `extractNoMatchReason(text)` — 提取冒号后的原因说明
  - 首次生成后检测 NO_MATCH → 直接返回友好错误提示
  - 纠错后也检测 NO_MATCH → 同样直接返回

**PromptTemplateService 精简（删除旧方法）：**
- 删除：`buildNl2SqlPrompt()` — 被 `buildSoftPrompt()` 替代
- 删除：`buildPromptByTemplateName()` 两个重载 — 不再需要按模板名选模板
- 删除：`buildCorrectionPrompt()` — 被 `buildCorrectionPromptWithHistory()` 替代
- 保留：`buildSoftPrompt()` + `buildCorrectionPromptWithHistory()` + `getTemplate()`
- init() 只加载2个模板：base_nl2sql + correction_nl2sql

---
### 前端界面改版v2：Gemini 风格（2026-03-18）
> 解决的问题：蓝白科技风不够高级，改为参照 Google Gemini 的界面风格
> 一句话：换新装（第二版）

**改动文件清单：**
- `frontend/src/App.vue` — 全面重写，Gemini 风格布局
- `frontend/src/components/Sidebar.vue` — 浅色侧栏
- `frontend/src/components/ChatMessage.vue` — Gemini 消息样式
- `frontend/src/components/DataSourceModal.vue` — Google 风格弹窗

**配色方案（Gemini 风格）**
| 元素 | 旧色值(蓝白科技风) | 新色值(Gemini风) |
|------|---------------------|------------------|
| 主背景 | #f0f4f8 (淡灰蓝渐变) | #ffffff (纯白) |
| 侧边栏 | #1e3a5f (深蓝) | #f8f9fa (浅灰白) |
| 主色调 | #3b82f6 (Tailwind蓝) | #1a73e8 (Google蓝) |
| 文字色 | #1e293b (深灰) | #1f1f1f (纯黑) |
| 次要文字 | #64748b | #5f6368 (Google灰) |
| 输入框 | 方角16px+蓝色边框 | 胶囊28px+灰底+聚焦蓝边 |
| 发送按钮 | 方角12px蓝渐变 | 圆形50% Google蓝 |
| 按钮风格 | 圆角12px | pill形 100px |
| AI头像 | 🤖 方角+蓝渐变 | ✦ 圆形+蓝渐变 |
| 用户头像 | 👤 紫色方角 | 👤 灰色圆形 |
| 示例卡片 | 2×2网格卡片 | 横排胶囊chip |
| 状态栏 | 独立一行 | 顶栏右侧pill |
| 错误提示 | rgba红色半透明 | #fce8e6 Google红 |
| 成功提示 | - | #e6f4ea Google绿 |

**视觉特效变化：**
- 移除：毛玻璃(backdrop-filter)、旋转光环动画、蓝色投影光晕
- 新增：极简纯白背景、Google Material Design 风格阴影
- 保留：hover 过渡动画、输入框聚焦高亮
- 连接状态：蓝色脉冲 → 绿色常亮LED
- 整体理念：从"科技酷炫"转变为"专业简洁"

---
## 七、参考项目
- E:\毕业设计\参考项目代码\Text2Sql_Agent-main（Python/Flask，数据源管理+模型管理）
- E:\毕业设计\参考项目代码\data-copilot-v2-master（Python/FastAPI，自纠错+并发+提示词工程）
---
## 八、关键配置速查
### application-dev.yml 关键配置
`yaml
n2sql:
  llm:
    api-key: YOUR_API_KEY_HERE      # DeepSeek API密钥
    base-url: https://api.deepseek.com/v1
    model-name: deepseek-chat
    temperature: 0.0                 # 0=精确，不要创意
    max-tokens: 2048
  correction:
    max-retries: 3                   # 自纠错最大重试次数
  sql:
    max-rows: 1000                   # 查询最大返回行数
    timeout-seconds: 30              # 查询超时秒数
`
### pom.xml 关键依赖
- spring-boot-starter-web (3.2.5)
- spring-boot-starter-validation
- spring-boot-starter-jdbc
- langchain4j + langchain4j-open-ai + langchain4j-spring-boot-starter (0.35.0)
- jsqlparser (4.9)
- mysql-connector-j + postgresql
- lombok

