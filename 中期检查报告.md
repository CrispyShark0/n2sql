# 杭州电子科技大学毕业设计（论文）中期检查报告

---

**题　　目**：基于大模型的自然语言数据库查询系统

**学　　院**：人文艺术与数字媒体学院

**专　　业**：数字媒体技术

**姓　　名**：张璐加

**班　　级**：22330111

**学　　号**：22330131

**指导教师**：聂斌玲

**报告日期**：2026年3月18日

---

## 一、课题研究目标与任务书要求回顾

### 1.1 课题目标

本课题旨在设计并实现一个基于大语言模型的自然语言数据库查询系统。系统以降低数据库使用门槛、提升数据获取与分析效率为目标，采用前后端分离架构，以 Web 应用形式为用户提供友好的交互界面，通过大语言模型实现自然语言到 SQL 查询语句的自动转换，并引入自验证机制提升生成结果的可靠性。

### 1.2 任务书核心要求

根据开题报告及导师的细化要求，本课题需完成以下核心工作：

1. **强化复杂 SQL 处理能力**：通过提示词工程处理多表联查、嵌套查询及窗口函数等复杂逻辑。
2. **自纠错与自反馈机制**：设计执行反馈闭环，生成的 SQL 执行报错时自动捕捉错误信息并反馈给大模型进行迭代修正。
3. **多数据库方言支持**：扩展对不同类型数据库（MySQL、PostgreSQL 等）的适配能力。
4. **前后端分离的 Web 系统**：后端基于 Spring Boot 框架，前端基于 Vue 框架，实现自然语言输入、查询过程展示及结果可视化。
5. **量化评测**：使用国际通用 Text-to-SQL 评测集量化系统执行准确率。

### 1.3 原定进度安排

| 序号 | 时间 | 计划内容 |
|:---:|------|----------|
| 5 | 2026.1.10 – 2026.3.10 | 课题设计、开发实施 |
| 6 | 2026.3.11 – 2026.3.20 | **中期检查** |
| 7 | 2026.3.21 – 2026.4.28 | 问题协调与解决、撰写毕业论文 |

---

## 二、已完成的工作及进度

截至 2026 年 3 月 18 日中期检查节点，系统的核心功能开发已全部完成，前后端均可正常运行，具备从"用户输入自然语言"到"返回数据库查询结果"的完整链路。以下按开发阶段逐一说明。

### 2.1 系统整体架构（已完成）

系统采用前后端分离架构，整体技术栈如下：

| 层次 | 技术选型 | 说明 |
|------|----------|------|
| 后端框架 | Java 17 + Spring Boot 3.2.5 | 模块化分层设计 |
| 大模型对接 | LangChain4j 0.35.0 + DeepSeek API | 兼容 OpenAI 格式 |
| SQL 验证 | JSQLParser 4.9 | 本地语法静态校验 |
| 数据库支持 | MySQL / PostgreSQL | 动态数据源+方言适配 |
| 前端框架 | Vue 3.4 + Vite 5 | 组件化 + 热更新 |
| HTTP 通信 | Axios + RESTful API | 前后端统一 JSON 交互 |

后端采用标准分层结构，共 25 个 Java 源文件，划分为配置层（config）、模型层（model）、业务服务层（service）、异常处理层（exception）、工具层（util）和控制器层（controller）六个模块。

**系统完整数据流如下图所示：**

```
用户输入自然语言问题 + 选择数据源
         │
         ▼
① SchemaExtractService：自动扫描数据库结构（表名、字段、类型、注释、主外键）→ DDL 文本
         │
         ▼
② QueryClassifier.detectHints()：软策略关键词检测 → 返回补充指令列表（hints）
         │
         ▼
③ PromptTemplateService.buildSoftPrompt()：基础模板 + Schema + hints + 用户问题 → 完整提示词
         │
         ▼
④ ChatLanguageModel：调用 DeepSeek 大模型 → 返回 SQL 文本
         │
         ▼
⑤ SqlCleanUtil：清洗 Markdown 格式、多余文字 → 干净 SQL
         │
         ▼
⑥ NO_MATCH 检测：若大模型返回"NO_MATCH: 原因" → 直接返回友好提示
         │（非 NO_MATCH）
         ▼
⑦ SqlValidateService：JSQLParser 语法校验 + Schema 一致性校验（Levenshtein 编辑距离纠错）
         │
    ┌────┴────┐
  失败       通过
    │         │
    │         ▼
    │    ⑧ SqlExecuteService：数据库真实执行
    │         │
    │    ┌────┴────┐
    │  失败       成功
    │    │         │
    ▼    ▼         ▼
  记录到完整纠错历史    返回查询结果 + 前端展示
  → 大模型重新生成
  （最多 3 次重试）
```

### 2.2 第一阶段：基础架构搭建（已完成）

完成了系统运行所需的全部基础设施：

- **动态数据源管理**：基于 HikariCP 连接池 + ConcurrentHashMap 实现运行时多数据库动态注册、获取连接和销毁，支持用户随时添加不同的数据库实例。
- **数据库结构自动提取**：通过 JDBC DatabaseMetaData API 自动扫描目标数据库的表名、列名、数据类型、注释、主键及外键关系，并格式化为 CREATE TABLE DDL 文本供大模型理解。
- **统一响应格式与异常处理**：ApiResult\<T\> 统一封装响应，GlobalExceptionHandler 全局捕获业务异常、参数校验异常和未知异常，保障接口返回的一致性。

### 2.3 第二阶段：大模型对接与基础 NL2SQL（已完成）

- **LLM 客户端配置**：通过 @ConfigurationProperties 自动绑定 yml 配置，创建 OpenAiChatModel Bean（兼容 DeepSeek API），temperature 设为 0.0 保证生成确定性。
- **提示词模板引擎**：设计 base_nl2sql.txt 基础模板，包含角色设定、Schema 信息、11 条约束规则和用户问题四部分。启动时加载到内存 HashMap 缓存，运行时通过 `{{schema}}`、`{{hints}}`、`{{question}}` 三个占位符进行字符串替换，生成完整提示词。
- **SQL 清洗工具**：处理大模型返回文本中的 Markdown 代码块标记、多余空白和末尾分号，提取干净的 SQL 语句。
- **安全执行引擎**：三重防护机制——仅允许 SELECT 语句、查询超时限制（30 秒）、返回行数限制（1000 行）。

### 2.4 第三阶段：自验证与自纠错机制（已完成）

这是本系统的核心创新点之一，实现了规则校验与执行反馈相结合的自验证机制：

- **静态验证（规则校验阶段）**：使用 JSQLParser 将 SQL 解析为抽象语法树（AST），检测括号不匹配、关键字拼写错误、语法结构不完整等问题。同时对照数据库 Schema 校验表名和列名是否存在，当检测到"字段幻觉"时，采用 Levenshtein 编辑距离算法自动计算并建议最相似的真实字段名。
- **动态验证（执行反馈阶段）**：将通过静态校验的 SQL 提交至数据库实际执行，根据返回的错误信息判断生成语句的逻辑或约束问题。
- **完整历史纠错闭环**（按导师反馈改进）：每次重试时，将之前**所有**失败的 SQL 和对应错误信息组装进纠错提示词，大模型能看到"之前尝试过什么、分别哪里出错了"，避免在多次重试之间来回振荡犯同样的错误。系统最多进行 3 次内部循环重试。

### 2.5 第四阶段：复杂 SQL 处理的软策略（已完成）

针对复杂查询（同时涉及聚合、多表联查、嵌套子查询等）的处理，经历了两个版本的迭代：

- **初始方案**：设计了 4 套专用提示词模板（简单/聚合/多表/嵌套），通过 QueryClassifier 进行互斥的硬分类，根据分类结果选择对应模板。
- **导师反馈后的改进方案（软策略）**：取消硬分类，改为始终使用一个基础模板兜底，同时通过关键词检测动态追加补充指令（hints）。三组关键词（聚合、多表、嵌套）的检测可以同时命中并叠加，解决了复杂问题"既有聚合又有多表又有嵌套"时硬分类只能选一个模板的问题。

### 2.6 第五阶段：多数据库方言适配（已完成）

- 使用 JDBC DatabaseMetaData.getDatabaseProductName() 自动检测数据库类型。
- 在提示词末尾动态注入方言提示（如 "Generate SQL compatible with MySQL syntax"）。
- 目前支持 MySQL 和 PostgreSQL 两种数据库。

### 2.7 第六阶段：REST API 接口与前端界面（已完成）

**后端接口（6 个 RESTful API）：**

| 方法 | 路径 | 功能 |
|------|------|------|
| POST | /api/datasource | 创建数据源 |
| GET | /api/datasource | 获取所有数据源 |
| GET | /api/datasource/{id} | 获取单个数据源 |
| DELETE | /api/datasource/{id} | 删除数据源 |
| POST | /api/datasource/test | 测试数据源连接 |
| POST | /api/nl2sql | 自然语言转 SQL 并执行（核心接口） |

**前端界面（Vue 3 + Vite 5）：**

采用 Google Gemini 风格设计，包含以下组件：
- **侧边栏（Sidebar.vue）**：数据源管理列表、新建对话按钮
- **主对话区（App.vue）**：欢迎页示例 + 对话式消息流
- **消息组件（ChatMessage.vue）**：SQL 代码高亮展示、查询结果表格、自纠错过程显示
- **数据源弹窗（DataSourceModal.vue）**：表单填写 + 连接测试

### 2.8 导师反馈改进（2026-03-18 完成）

根据导师对系统方案的审核意见，完成了以下三项重要改进：

| 导师要求 | 实现方式 | 状态 |
|----------|----------|------|
| Schema 多提取字段类型和注释 | ColumnInfo 已含 dataType + comment，原本就已满足 | ✅ |
| 意图分类改为软策略 | QueryClassifier.detectHints() 返回 List\<String\> 补充指令 | ✅ |
| 重试时传完整历史上下文 | buildCorrectionPromptWithHistory() 传入全部 CorrectionRecord | ✅ |
| 不存在实体返回 NO_MATCH | prompt 规则约束 + isNoMatch() 代码检测 | ✅ |

### 2.9 NO_MATCH 实体不存在检测（2026-03-18 新增）

当用户查询的实体在数据库中不存在时（如查询"学生"但数据库只有"员工"表），大模型可能会"猜测"并生成错误的 SQL。为解决此问题，在提示词规则中明确约束：找不到对应实体时必须返回 `NO_MATCH: <原因>`，系统代码检测到该标识后直接返回友好错误提示，不会猜测执行。

---

## 三、实测验证情况

### 3.1 测试环境

- 测试数据库：MySQL 8.0，数据库名 n2sql_test
- 测试数据：电商销售场景，包含 6 张表（departments、employees、customers、products、orders、order_items），含主外键关系和中文注释
- 大模型：DeepSeek-Chat（通过 OpenAI 兼容 API 调用）

### 3.2 端到端测试结果

| 测试用例 | 查询类型 | 是否一次成功 |
|----------|----------|:---:|
| 查询所有员工的姓名和工资 | 简单查询 | ✅ |
| 每个部门的平均工资从高到低排列 | 聚合 + JOIN + 排序 | ✅ |
| 购买金额最多的前 3 个客户 | 多表联查 + TOP N | ✅ |
| 工资高于公司平均工资的员工 | 嵌套子查询 | ✅ |

### 3.3 编译与构建状态

- 后端：25 个 Java 源文件，`mvn compile` 零编译错误
- 前端：4 个 Vue 组件 + API 封装，`vite build` 零构建错误
- 总代码量：约 2500+ 行 Java 代码 + 前端 Vue/CSS 代码 + 提示词模板

---

## 四、关键技术方案说明

### 4.1 提示词工程——软策略动态注入

系统始终使用一个基础提示词模板（base_nl2sql.txt），通过 `{{hints}}` 占位符实现动态扩展。QueryClassifier 组件对用户问题进行三组关键词检测（聚合类、多表类、嵌套类），各组检测互不排斥，检测到的补充指令可同时叠加注入模板。例如，"查询销售额最高的产品类别及其最畅销产品"会同时命中聚合和多表两组指令。

### 4.2 两级验证——静态校验 + 动态执行

第一级为 JSQLParser 本地静态校验，将 SQL 解析为 AST 检查语法正确性，并结合 Schema 信息校验表名和列名的存在性。当检测到字段幻觉时，使用 Levenshtein 编辑距离算法推荐最相似的真实列名（编辑距离 ≤ 3 时推荐）。

第二级为数据库实际执行，捕获运行时错误。两级验证的错误信息均记入完整纠错历史，反馈给大模型进行迭代修正。

### 4.3 完整历史纠错——避免振荡

传统纠错方案仅传入上一次的错误 SQL 和错误信息，容易导致模型在两个错误之间来回振荡。本系统改为传入所有历史错误记录（格式化为 "Attempt 1: SQL=..., Error=..."），大模型能全面了解之前的失败尝试，有效避免重复犯错。

---

## 五、与开题报告计划的对比分析

### 5.1 进度对比

| 计划内容 | 计划时间 | 实际完成时间 | 状态 |
|----------|----------|-------------|------|
| 课题设计与核心功能开发 | 2026.1.10 – 2026.3.10 | 2026.1 – 2026.3.4 | ✅ 按时完成 |
| Bug 修复与功能优化 | — | 2026.3.16 | ✅ 已修复 |
| 导师反馈改进 | — | 2026.3.18 | ✅ 已完成 |
| 前端界面开发 | 包含在第5项中 | 2026.3.16 – 2026.3.18 | ✅ 已完成 |
| 中期检查 | 2026.3.11 – 2026.3.20 | 2026.3.18 | ✅ 按时 |

### 5.2 开题报告目标完成度

| 开题报告研究内容 | 完成度 | 说明 |
|------------------|:------:|------|
| 前后端分离架构系统设计与实现 | 100% | 后端 Spring Boot + 前端 Vue 3 |
| 多数据源接入与管理 | 100% | 动态 HikariCP 连接池 + CRUD 接口 |
| 大模型自然语言到 SQL 自动转换 | 100% | DeepSeek + 提示词模板引擎 |
| 结果自验证机制（规则校验 + 执行反馈） | 100% | JSQLParser 静态校验 + 数据库动态执行 + 纠错闭环 |
| 查询结果表格化展示 | 100% | 前端 ChatMessage 组件表格渲染 |
| 多数据库方言适配 | 80% | 已支持 MySQL/PostgreSQL，ClickHouse 等待后续扩展 |
| 量化评测（Spider/BIRD） | 0% | 待第七阶段实施 |

### 5.3 技术方案与开题报告的调整说明

在实际开发过程中，根据导师反馈对以下技术方案进行了优化调整：

1. **后端框架调整**：开题报告原计划使用 Flask（Python），实际改用 Spring Boot（Java），以配合本科教学体系。
2. **意图分类策略调整**：从最初的 4 套专用模板硬分类，调整为基于基础模板的软策略动态注入，避免误分类带来的负面效果。
3. **纠错上下文策略调整**：从仅传入最近一次错误，调整为传入完整历史错误链，减少模型在多次重试间的振荡。

---

## 六、后续工作计划

| 时间 | 工作内容 | 优先级 |
|------|----------|--------|
| 2026.3.21 – 2026.3.31 | 第七阶段：Spider/BIRD 评测集量化测试，获取执行准确率数据 | 高 |
| 2026.4.1 – 2026.4.10 | 对比实验与消融实验：验证各模块（分类、纠错、方言）的贡献度 | 高 |
| 2026.4.1 – 2026.4.15 | 撰写毕业论文初稿 | 高 |
| 2026.4.15 – 2026.4.28 | 论文修改、系统优化、功能完善 | 中 |
| 2026.4.29 – 2026.5.6 | 论文评审及查重 | — |
| 2026.5.7 – 2026.5.13 | 答辩报告会 | — |

---

## 七、当前存在的问题及解决思路

| 问题 | 影响 | 解决思路 |
|------|------|----------|
| 缺少量化评测数据 | 无法证明系统准确率 | 下一阶段在 Spider 评测集上批量测试并统计 EX/EM 指标 |
| 数据源信息未持久化 | 重启后数据丢失 | 考虑增加文件持久化或嵌入式数据库 |
| 大表 Schema 可能超出 token 限制 | 影响大规模数据库的适用性 | 考虑 Schema 压缩或相关表筛选策略 |

---

## 八、总结

截至中期检查节点，本课题的**系统设计与核心功能开发已全部完成**，开发进度符合任务书要求。系统已实现自然语言查询、SQL 自动生成、两级自验证、完整历史纠错闭环、多数据库方言适配以及 Web 前端交互等核心功能。同时根据导师反馈完成了软策略、完整历史纠错、NO_MATCH 检测三项重要改进。

后续工作将重点围绕**量化评测**（Spider/BIRD 评测集测试）和**毕业论文撰写**展开，按照任务书进度安排有序推进。

---

*本报告由张璐加撰写，指导教师：聂斌玲*
